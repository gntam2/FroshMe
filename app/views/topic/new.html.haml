%h2 Create a New Discussion Topic 
-if !current_user.nil?
	=form_for Topics.new, :url => "/topic" do |f|
		=f.label :Name
		=f.text_field :name
		%br 
		%br
		=f.label :Institution 
		=collection_select(:topics, :institution_id, Institution.all, :id, :name)
		%br
		%br
		=f.submit
-else
	%p Please Sign in to create a topic
